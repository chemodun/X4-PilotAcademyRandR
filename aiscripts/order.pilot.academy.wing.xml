<?xml version="1.0" encoding="utf-8"?>
<aiscript name="order.pilot.academy.wing" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="aiscripts.xsd" version="5">
  <order id="PilotAcademyWing" name="{1972092412,101}" description="{1972092412,102}" category="trade" infinite="true" allowinloop="false">
    <params>
      <param name="wingId" type="internal" text="Wing ID" comment="Wing ID" default="''" />
      <param name="runMode" type="internal" text="Run Mode" comment="Run Mode" default="false" />
      <param name="debugchance" type="bool"
        default="if @player.entity.$pilotAcademyRAndRData.$debugLevel == 'debug' or @player.entity.$pilotAcademyRAndRData.$debugLevel == 'trace' then 100 else 0"
        advanced="true" text="{1041, 10086}" comment="Print debug output">
        <input_param name="truevalue" value="100" />
      </param>
    </params>
    <requires>
      <match shiptype="shiptype.lasertower" negate="true" />
      <match class="class.spacesuit" negate="true" />
      <match class="class.ship_s" />
    </requires>
    <location object="@$tradeTarget" condition="@$tradeTarget != null" />
    <location object="this.assignedcontrolled" condition="@$tradeTarget == null" />
  </order>
  <interrupts>
    <library>
      <actions name="PilotAcademyGetTargetRankLevel">
        <set_value name="$targetRankLevel" exact="3" />
        <set_value name="$commonData" exact="@player.entity.$pilotAcademyRAndRData" />
        <debug_text text="'Pilot Academy [%s]: loaded commonData: %s' . [this.assignedcontrolled.debugname, @$commonData]" chance="$debugchance" />
        <do_if value="@$commonData != null and @$commonData.$targetRankLevel != null">
          <set_value name="$targetRankLevel" exact="@$commonData.$targetRankLevel" />
        </do_if>
        <debug_text
          text="'Pilot Academy [%s]: determined targetRankLevel: %s' . [this.assignedcontrolled.debugname, $targetRankLevel]"
          chance="$debugchance" />
      </actions>
      <actions name="PilotAcademyProcessWingData">
        <set_value name="$wingData" exact="null" />
        <set_value name="$wingsData" exact="@player.entity.$pilotAcademyRAndRWings" />
        <do_if value="typeof $wingsData == datatype.table">
          <set_value name="$wingData" exact="@$wingsData.{'$%s'.[$wingId]}" />
        </do_if>
        <debug_text
          text="'Pilot Academy [%s]: loaded wingData: %s for wingId: %s from wingsData: %s' . [this.assignedcontrolled.debugname, @$wingData, @$wingId, @$wingsData]"
          chance="$debugchance" />
        <do_if value="@$wingData != null">
          <set_value name="$factionNames" exact="[]" />
          <do_if value="$wingData.$factions? and @$wingData.$factions != null and typeof $wingData.$factions == datatype.list">
            <set_value name="$factionNames" exact="$wingData.$factions" />
          </do_if>
          <set_value name="$primaryGoal" exact="'rank'" />
          <do_if value="$wingData.$primaryGoal? and @$wingData.$primaryGoal != null">
            <set_value name="$primaryGoal" exact="$wingData.$primaryGoal" />
          </do_if>
          <set_value name="$tradeInfoRefreshInterval" exact="1800s" />
          <do_if value="$wingData.$refreshInterval? and @$wingData.$refreshInterval != null">
            <set_value name="$tradeInfoRefreshInterval" exact="(@$wingData.$refreshInterval * 60)s" />
          </do_if>
          <debug_text
            text="'Pilot Academy [%s]: extracted from wingData: factionNames count: %s, primaryGoal: %s, tradeInfoRefreshInterval: %s' .
              [this.assignedcontrolled.debugname, $factionNames.count, $primaryGoal, $tradeInfoRefreshInterval]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyGetTargetRankLevel" />
        </do_if>
        <do_else>
          <set_value name="$runMode" exact="false" />
        </do_else>
      </actions>
      <actions name="PilotAcademyInitShipVariables">
        <set_value name="$idCode" exact="this.assignedcontrolled.idcode" />
        <debug_text
          text="'Pilot Academy [%s]: pilotAcademyRAndRWingsInfo: %s' . [this.assignedcontrolled.debugname, @player.entity.$pilotAcademyRAndRWingsInfo]"
          chance="$debugchance" />
        <do_if value="not player.entity.$pilotAcademyRAndRWingsInfo? or @player.entity.$pilotAcademyRAndRWingsInfo == null">
          <set_value name="player.entity.$pilotAcademyRAndRWingsInfo" exact="table[]" />
        </do_if>
        <set_value name="$wingsInfo" exact="player.entity.$pilotAcademyRAndRWingsInfo" />
        <debug_text text="'Pilot Academy [%s]: loaded wingsInfo: %s, shipsInfo: %s' . [this.assignedcontrolled.debugname, @$wingsInfo, @$wingsInfo.{'$%s'.[$wingId]}]" chance="$debugchance" />
        <do_if value="not $wingsInfo.{'$%s'.[$wingId]}? or @$wingsInfo.{'$%s'.[$wingId]} == null">
          <set_value name="$wingsInfo.{'$%s'.[$wingId]}" exact="table[]" />
        </do_if>
        <set_value name="$shipsInfo" exact="$wingsInfo.{'$%s'.[$wingId]}" />
        <debug_text text="'Pilot Academy [%s]: loaded shipsInfo: %s, shipInfo: %s' . [this.assignedcontrolled.debugname, @$shipsInfo, @$shipsInfo.{'$%s'.[$idCode]}]" chance="$debugchance" />
        <do_if value="not $shipsInfo.{'$%s'.[$idCode]}? or @$shipsInfo.{'$%s'.[$idCode]} == null">
          <set_value name="$shipsInfo.{'$%s'.[$idCode]}" exact="table[]" />
        </do_if>
        <set_value name="$shipInfo" exact="@$shipsInfo.{'$%s'.[$idCode]}" />
        <do_if value="not $shipInfo.$dataIsUpdated? or @$shipInfo.$dataIsUpdated == null">
          <set_value name="$shipInfo.$dataIsUpdated" exact="false" />
        </do_if>
        <debug_text
          text="'Pilot Academy [%s]: initialized shipInfo: %s, for idcode: %s.' . [this.assignedcontrolled.debugname, @$shipInfo, $idCode]"
          chance="$debugchance" />
        <remove_value name="$idCode" />
      </actions>
      <actions name="PilotAcademyRemoveShipVariables">
        <set_value name="$ship" exact="if @event.object == null then this.assignedcontrolled else @event.object" />
        <set_value name="$idCode" exact="$ship.idcode" />
        <do_if value="$shipsInfo? and @$shipsInfo!= null and $shipsInfo.{'$%s'.[$idCode]}? and @$shipsInfo.{'$%s'.[$idCode]} != null">
          <remove_value name="$shipsInfo.{'$%s'.[$idCode]}" />
          <debug_text
            text="'Pilot Academy [%s]: removed shipInfo for idcode: %s.' . [$ship, $idCode]"
            chance="$debugchance" />
        </do_if>
        <remove_value name="$idCode" />
        <remove_value name="$ship" />
      </actions>
      <actions name="PilotAcademyStoreCurrentShipInfo">
        <set_value name="$shipInfo.$tradeInfo"
          exact="table[
            $timeStamp = player.age,
            $tradeOperation = $tradeOperation,
            $selectedFaction = $selectedFaction,
            $selectedBuyOffer = $selectedBuyOffer,
            $selectedSellOffer = $selectedSellOffer,
            $lastTradeInfoRefresh = $lastTradeInfoRefresh
          ]" />
      </actions>
      <actions name="PilotAcademyRecoverCurrentShipInfo">
        <debug_text
          text="'Pilot Academy [%s]: check stored info in $shipInfo.$tradeInfo: %s, (%s, %s)'.[this.assignedcontrolled.debugname, @$shipInfo.$tradeInfo, $shipInfo.$tradeInfo?, $shipInfo.$tradeInfo.$timeStamp?]"
          chance="$debugchance" />
        <do_if value="$shipInfo.$tradeInfo? and $shipInfo.$tradeInfo.$timeStamp?">
          <set_value name="$lastTradeInfo" exact="$shipInfo.$tradeInfo" />
          <debug_text
            text="'Pilot Academy [%s]: found last trade info at %s ago: %s'.[this.assignedcontrolled.debugname, player.age - $lastTradeInfo.$timeStamp, $lastTradeInfo]"
            chance="$debugchance" />
          <do_if value="(player.age - $lastTradeInfo.$timeStamp) lt 180s">
            <set_value name="$tradeOperation" exact="$lastTradeInfo.$tradeOperation" />
            <set_value name="$selectedFaction" exact="$lastTradeInfo.$selectedFaction" />
            <set_value name="$selectedBuyOffer" exact="$lastTradeInfo.$selectedBuyOffer" />
            <set_value name="$selectedSellOffer" exact="$lastTradeInfo.$selectedSellOffer" />
            <set_value name="$lastTradeInfoRefresh" exact="$lastTradeInfo.$lastTradeInfoRefresh" />
            <debug_text
              text="'Pilot Academy [%s]: reusing last trade info from %s ago. Completed %s on %s.' . [this.assignedcontrolled.debugname, player.age - $lastTradeInfo.$timeStamp, $tradeOperation, if @$tradeOperation == 'sell' then @$selectedBuyOffer.owner.debugname else @$selectedSellOffer.owner.debugname]"
              chance="$debugchance" />
          </do_if>
          <remove_value name="$lastTradeInfo" />
          <remove_value name="$shipInfo.$tradeInfo" />
        </do_if>
      </actions>
      <actions name="PilotAcademyResetCurrentTradeInfo">
        <include_interrupt_actions ref="PilotAcademyInitShipVariables" />
        <remove_value name="$shipInfo.$tradeInfo" />
        <set_value name="$selectedFaction" exact="null" />
        <set_value name="$selectedBuyOffer" exact="null" />
        <set_value name="$selectedSellOffer" exact="null" />
        <set_value name="$lastTradeInfoRefresh" exact="0s" />
        <set_value name="$tradeOperation" exact="''" />
      </actions>
      <actions name="PilotAcademyPrintStats">
        <debug_text
          text="'Pilot Academy [%s]: *PilotAcademyPrintStats*:  faction %s relation: %s (%s), pilot skills - combined: %s, Pilot: piloting %s, morale %s, management %s, engineering %s, boarding %s'.
            [this.assignedcontrolled.debugname, @$selectedFaction, @$selectedFaction.relationto.{faction.player}, @$selectedFaction.relation.{@$selectedFaction.relationto.{faction.player}}.uivalue, @this.combinedskill, @this.skill.{skilltype.piloting}, @this.skill.{skilltype.morale}, @this.skill.{skilltype.management}, @this.skill.{skilltype.engineering}, @this.skill.{skilltype.boarding}]"
          chance="$debugchance" />
      </actions>
      <actions name="PilotAcademyShareTradeInfo">
        <set_value name="$isShareInfoValid" exact="true" />
        <do_if value="@$shareInfoTarget != null and @$selectedFaction != null and @$selectedBuyOffer != null and  @$selectedBuyOffer.exists and @$selectedSellOffer != null and @$selectedSellOffer.exists">
          <signal_objects object="$shareInfoTarget" param="'PilotAcademyRAndR.TradeInfoDataUpdatedSignal'" param2="this.assignedcontrolled"
            param3="table[
              $selectedFaction = $selectedFaction,
              $selectedBuyOffer = $selectedBuyOffer,
              $selectedSellOffer = $selectedSellOffer
              ]"
            delay="300ms" />
          <debug_text
            text="'Pilot Academy [%s]: shared trade info with %s: faction %s, buy offer %s, sell offer %s'.[this.assignedcontrolled.debugname, $shareInfoTarget.debugname, @$selectedFaction.name, @$selectedBuyOffer.ware.id, @$selectedSellOffer.ware.id]"
            chance="$debugchance" />
        </do_if>
        <do_else>
          <set_value name="$isShareInfoValid" exact="true" />
        </do_else>
      </actions>
      <actions name="PilotAcademyReportRankUp">
        <do_if
          value="this.combinedskill ge $targetCombinedSkill and (not $shipInfo.$LastRankUpReportTime? or (player.age - $shipInfo.$LastRankUpReportTime) ge 180s)">
          <debug_text
            text="'Pilot Academy [%s]: Pilot %s reached target rank level %s with combined skill %s, reporting to Academy to request replacement.' .
              [this.assignedcontrolled.debugname, this.debugname, $targetRankLevel, this.combinedskill]"
            chance="$debugchance" />
          <set_value name="$messageTitle" exact="{1972092412,1}" />
          <set_value name="$messageText" exact="{1972092412,1000001}.[this.name, $targetRankLevel]" />
          <set_value name="$messageObject" exact="this.assignedcontrolled" />
          <include_interrupt_actions ref="PilotAcademyNotify" />
          <do_if value="this.assignedcontrolled.people.free ge 1">
            <raise_lua_event name="'PilotAcademyRAndR.RankLevelReached'" param="this.assignedcontrolled" />
            <set_value name="$rankUpReported" exact="true" />
          </do_if>
          <do_else>
            <debug_text
              text="'Pilot Academy [%s]: Ship has no free crew capacity to perform rank up replacement!' .
                [this.assignedcontrolled.debugname]"
              chance="$debugchance" />
            <set_value name="$messageTitle" exact="{1972092412,1}" />
            <set_value name="$messageText" exact="{1972092412,2000002}.[this.assignedcontrolled.name]" />
            <set_value name="$messageObject" exact="this.assignedcontrolled" />
            <set_value name="$isMessageWarning" exact="true" />
            <include_interrupt_actions ref="PilotAcademyNotify" />
            <set_value name="$rankUpReported" exact="false" />
          </do_else>
          <include_interrupt_actions ref="PilotAcademyInitShipVariables" />
          <include_interrupt_actions ref="PilotAcademyStoreCurrentShipInfo" />
          <set_value name="$shipInfo.$LastRankUpReportTime" exact="player.age" />
        </do_if>
      </actions>
      <actions name="PilotAcademySearchForDemandOffers">
        <set_value name="$demandOffers" exact="null" />
        <do_if value="$offerType == 'buy'">
          <find_buy_offer tradepartner="this.assignedcontrolled" space="player.galaxy" result="$demandOffers" multiple="true">
            <totalvolume min="$tradeVolumeMin" />
            <match_buyer tradesknownto="this.owner" owner="$faction">
              <match_relation_to object="this.assignedcontrolled" relation="enemy" comparison="not" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.objectactivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectoractivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectortravel" object="this.assignedcontrolled" />
            </match_buyer>
          </find_buy_offer>
        </do_if>
        <do_else>
          <find_sell_offer tradepartner="this.assignedcontrolled" space="player.galaxy" result="$demandOffers" multiple="true">
            <totalvolume min="$tradeVolumeMin" />
            <match_seller tradesknownto="this.owner" owner="$faction">
              <match_relation_to object="this.assignedcontrolled" relation="enemy" comparison="not" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.objectactivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectoractivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectortravel" object="this.assignedcontrolled" />
            </match_seller>
          </find_sell_offer>
        </do_else>
        <debug_text
          text="'Pilot Academy [%s]: found %s %s offers from faction %s' . [this.assignedcontrolled.debugname, @$demandOffers.count, $offerType, @$faction.name]"
          chance="$debugchance" />
      </actions>
      <actions name="PilotAcademySearchForSupplyOffers">
        <set_value name="$supplyOffers" exact="null" />
        <do_if value="$offerType == 'buy'">
          <find_sell_offer tradepartner="this.assignedcontrolled" space="player.galaxy" result="$supplyOffers" wares="$demandOffer.ware"
            multiple="true">
            <totalvolume min="$tradeVolumeMin" />
            <match_seller tradesknownto="this.owner">
              <match_relation_to object="this.assignedcontrolled" relation="enemy" comparison="not" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.objectactivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectoractivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectortravel" object="this.assignedcontrolled" />
            </match_seller>
          </find_sell_offer>
        </do_if>
        <do_else>
          <find_buy_offer tradepartner="this.assignedcontrolled" space="player.galaxy" result="$supplyOffers" wares="$demandOffer.ware"
            multiple="true">
            <totalvolume min="$tradeVolumeMin" />
            <match_buyer tradesknownto="this.owner">
              <match_relation_to object="this.assignedcontrolled" relation="enemy" comparison="not" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.objectactivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectoractivity" object="this.assignedcontrolled" />
              <match_use_blacklist group="$blacklistGroup" type="blacklisttype.sectortravel" object="this.assignedcontrolled" />
            </match_buyer>
          </find_buy_offer>
        </do_else>
        <debug_text
          text="'Pilot Academy [%s]: found %s opposite offers to %s of %s' . [this.assignedcontrolled.debugname, @$supplyOffers.count, $offerType, $demandOffer.ware.id]"
          chance="$debugchance" />
      </actions>
      <actions name="PilotAcademyDebugApply">
        <debug_text
          text="'Pilot Academy [%s]: Debug Apply interrupt actions invoked. Is Init: %s. Current debug chance: %s. Debug level: %s' .
          [this.assignedcontrolled.debugname, @$isInit, $debugchance, @player.entity.$pilotAcademyRAndRData.$debugLevel]"
        />
        <do_if value="@$isInit == null or @$isInit != true">
          <set_value name="$debugchance" exact="0" />
        </do_if>
        <do_if value="@player.entity.$pilotAcademyRAndRData.$debugLevel == 'debug' or @player.entity.$pilotAcademyRAndRData.$debugLevel == 'trace'">
          <set_value name="$debugchance" exact="100" />
          <debug_text
            text="'Pilot Academy [%s]: Debug level is %s, setting debug chance to %s.' .
            [this.assignedcontrolled.debugname, @player.entity.$pilotAcademyRAndRData.$debugLevel, $debugchance]"
            chance="$debugchance"
          />
        </do_if>
      </actions>
      <actions name="PilotAcademyNotify">
        <do_if value="@$commonData.$notificationsEnabled == true">
          <do_if value="@$isMessageWarning == true">
            <show_notification text="'%s\n%s'.[$messageTitle, $messageText]" priority="2" sound="notification_warning"
              timeout="15s" />
          </do_if>
          <do_else>
            <show_notification text="'%s\n%s'.[$messageTitle, $messageText]" timeout="15s" />
          </do_else>
        </do_if>
        <do_if value="@$commonData.$logbookEnabled == true">
          <do_if value="@$messageMoney != null">
            <write_to_logbook category="general" title="$messageTitle" text="$messageText"
              interaction="showonmap" object="$messageObject" highlighted="@$isMessageWarning == true" money="$messageMoney" />
          </do_if>
          <do_else>
            <write_to_logbook category="general" title="$messageTitle" text="$messageText"
              interaction="showonmap" object="$messageObject" highlighted="@$isMessageWarning == true" />
          </do_else>
        </do_if>
        <remove_value name="$messageTitle" />
        <remove_value name="$messageText" />
        <remove_value name="$messageObject" />
        <remove_value name="$messageMoney" />
        <remove_value name="$isMessageWarning" />
      </actions>
      <actions name="PilotAcademyGetIllegalWaresForStation">
        <get_wares_illegal_to result="$illegalWaresForStation" faction="@$stationForTrade.owner" />
        <set_value name="$stationForTradeSector" exact="@$stationForTrade.sector" />
        <debug_text text="'Pilot Academy [%s]: found illegal wares count: %s for faction: %s, owner of station: %s in sector %s with owner: %s' .
            [this.assignedcontrolled.debugname, @$illegalWaresForStation.count, @$stationForTrade.owner, @$stationForTrade.debugname, @$stationForTradeSector.debugname, @$stationForTradeSector.owner]"
          chance="$debugchance" />
        <do_if value="@$stationForTrade.owner != @$stationForTrade.trueowner">
          <get_wares_illegal_to result="$illegalWaresForStation" faction="@$stationForTrade.trueowner" />
          <debug_text text="'Pilot Academy [%s]: found illegal wares count: %s for faction - true owner: %s of station: %s in sector %s with owner: %s' .
            [this.assignedcontrolled.debugname, @$illegalWaresForStation.count, @$stationForTrade.trueowner, @$stationForTrade.debugname, @$stationForTradeSector.debugname, @$stationForTradeSector.owner]"
            chance="$debugchance" />
        </do_if>
        <do_if value="@$stationForTradeSector.owner != null and @$stationForTradeSector.owner != @$stationForTrade.owner">
          <get_wares_illegal_to result="$illegalWaresSectorOwner" faction="@$stationForTradeSector.owner" />
          <debug_text text="'Pilot Academy [%s]: found illegal wares count: %s for sector %s owner: %s' .
            [this.assignedcontrolled.debugname, @$illegalWaresSectorOwner.count, @$stationForTradeSector.debugname, @$stationForTradeSector.owner]"
            chance="$debugchance" />
          <append_list_elements name="$illegalWaresForStation" other="$illegalWaresSectorOwner" />
          <do_if value="@$stationForTradeSector.owner != @$stationForTradeSector.trueowner">
            <get_wares_illegal_to result="$illegalWaresSectorOwner" faction="@$stationForTradeSector.trueowner" />
            <debug_text text="'Pilot Academy [%s]: found illegal wares count: %s for sector %s true owner: %s' .
              [this.assignedcontrolled.debugname, @$illegalWaresSectorOwner.count, @$stationForTradeSector.debugname, @$stationForTradeSector.trueowner]"
              chance="$debugchance" />
            <append_list_elements name="$illegalWaresForStation" other="$illegalWaresSectorOwner" />
          </do_if>
        </do_if>
        <remove_value name="$stationForTradeSector" />
      </actions>
      <actions name="PilotAcademyCheckTradesBlacklist">
        <set_value name="$commonData" exact="@player.entity.$pilotAcademyRAndRData" />
        <do_if value="@$commonData.$tradesBlacklist == null or typeof @$commonData.$tradesBlacklist != datatype.table">
          <set_value name="$commonData.$tradesBlacklist" exact="table[]" />
        </do_if>
        <set_value name="$blackList" exact="@$commonData.$tradesBlacklist" />
        <set_value name="$isBlackListed" exact="false" />
        <do_if value="@$blackList.keys.count gt 0">
          <set_value name="$tradeRecordId" exact="'$%s-%s-%s'.[$blackListCheckOperation, $blackListCheckOffer.owner.idcode, $blackListCheckOffer.ware.id]" />
          <do_if value="@$blackList.{$tradeRecordId} != null">
            <set_value name="$tradeRecord" exact="@$blackList.{$tradeRecordId}" />
            <debug_text
              text="'Pilot Academy [%s]: found trade record in blacklist for operation: %s, station: %s, ware: %s. Record details: %s. Time delta: %s, threshold: %s' .
                [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.owner.debugname, $blackListCheckOffer.ware.id, $tradeRecord, player.age - $tradeRecord.$timeStamp, $tradeInfoRefreshInterval * 2]"
              chance="$debugchance" />
            <do_if value="(player.age - $tradeRecord.$timeStamp) gt ($tradeInfoRefreshInterval * 2)">
              <debug_text
                text="'Pilot Academy [%s]: trade record in blacklist is expired, removing from blacklist. Record details: %s' .
                    [this.assignedcontrolled.debugname, $tradeRecord]"
                chance="$debugchance" />
              <remove_value name="$blackList.{$tradeRecordId}" />
            </do_if>
            <do_else>
              <set_value name="$isBlackListed" exact="true" />
            </do_else>
            <remove_value name="$tradeRecord" />
          </do_if>
        </do_if>
      </actions>
      <actions name="PilotAcademyAddTradeToBlacklist">
        <set_value name="$commonData" exact="@player.entity.$pilotAcademyRAndRData" />
        <do_if value="@$commonData.$tradesBlacklist == null or typeof @$commonData.$tradesBlacklist != datatype.table">
          <set_value name="$commonData.$tradesBlacklist" exact="table[]" />
        </do_if>
        <set_value name="$blackList" exact="@player.entity.$pilotAcademyRAndRData.$tradesBlacklist" />
        <set_value name="$blacklistItemId" exact="'$%s-%s-%s'.[$blackListAddOperation, $blackListAddOffer.owner.idcode, $blackListAddOffer.ware.id]" />
        <set_value name="$blacklistItem" exact="@$blackList.{$blacklistItemId}" />
        <debug_text text="'Pilot Academy [%s]: going to add trade record to blacklist for operation: %s, station: %s, ware: %s. Blacklist: %s, Item: %s.' .
          [this.assignedcontrolled.debugname, $blackListAddOperation, $blackListAddOffer.owner.debugname, $blackListAddOffer.ware.id, $blackList, $blacklistItem]"
          chance="$debugchance" />
        <do_if value="@$blacklistItem == null">
          <set_value name="$blackList.{$blacklistItemId}" exact="table[
            $timeStamp = player.age
          ]" />
          <debug_text
            text="'Pilot Academy [%s]: added trade record to blacklist for operation: %s, station: %s, ware: %s. Blacklist: %s, Item: %s.' .
              [this.assignedcontrolled.debugname, $blackListAddOperation, $blackListAddOffer.owner.debugname, $blackListAddOffer.ware.id, $blackList, $blacklistItem]"
            chance="$debugchance" />
          <do_if value="this.assignedcontrolled.commander != null">
            <signal_objects object="this.assignedcontrolled.commander" param="'PilotAcademyRAndR.TradesBlacklistUpdatedSignal'" param2="this.assignedcontrolled" param3="$blackListAddOffer.ware" />
          </do_if>
        </do_if>
        <remove_value name="$blackListAddOperation" />
        <remove_value name="$blackListAddOffer" />
      </actions>
      <actions name="PilotAcademyCheckAndClearReservation">
        <do_if value="@$tradeTarget != null">
          <do_if value="@$tradeOperation == 'buy'">
            <get_ware_reservation object="$tradeTarget" type="buy" ware="$tradeWare" reserver="this.assignedcontrolled" result="$reservationResult" />
            <debug_text text="'Pilot Academy [%s]: checking ware %s reservation for buy trade, result: %s' . [this.assignedcontrolled.debugname, $tradeWare.id, $reservationResult]" chance="$debugchance" />
            <do_if value="$reservationResult gt 0">
              <remove_ware_reservation object="$tradeTarget" type="buy" ware="$tradeWare" reserver="this.assignedcontrolled" />
              <debug_text text="'Pilot Academy [%s]: removed ware %s reservation for buy trade' . [this.assignedcontrolled.debugname, $tradeWare.id]" chance="$debugchance" />
            </do_if>
          </do_if>
          <do_elseif value="@$tradeOperation == 'sell'">
            <get_ware_reservation object="$tradeTarget" type="sell" ware="$tradeWare" reserver="this.assignedcontrolled" result="$reservationResult" />
            <debug_text text="'Pilot Academy [%s]: checking ware %s reservation for sell trade, result: %s' . [this.assignedcontrolled.debugname, $tradeWare.id, $reservationResult]" chance="$debugchance" />
            <do_if value="$reservationResult gt 0">
              <remove_ware_reservation object="$tradeTarget" type="sell" ware="$tradeWare" reserver="this.assignedcontrolled" />
              <debug_text text="'Pilot Academy [%s]: removed ware %s reservation for sell trade' . [this.assignedcontrolled.debugname, $tradeWare.id]" chance="$debugchance" />
            </do_if>
          </do_elseif>
        </do_if>
      </actions>
    </library>
    <handler ref="AttackHandler" />
    <handler ref="MissileLockHandler" />
    <handler ref="ScannedHandler" />
    <handler ref="InspectedHandler" />
    <handler ref="FoundAbandonedHandler" />
    <handler ref="FoundLockboxHandler" />
    <handler ref="ResupplyHandler" />
    <handler ref="TideHandler" />
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.TradeInfoDataUpdatedSignal'"
          param2="this.assignedcontrolled.commander" />
        <check_value value="@event.param3 != null" />
        <check_value value="@event.param3.$selectedFaction != null" />
        <check_value value="@event.param3.$selectedBuyOffer != null" />
        <check_value value="@event.param3.$selectedSellOffer != null" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received trade info update from commander %s'.[this.assignedcontrolled.debugname, event.param2.debugname]"
          chance="$debugchance" />
        <set_value name="$selectedFaction" exact="event.param3.$selectedFaction" />
        <set_value name="$selectedBuyOffer" exact="event.param3.$selectedBuyOffer" />
        <set_value name="$selectedSellOffer" exact="event.param3.$selectedSellOffer" />
        <set_value name="$tradeOperation" exact="''" />
        <abort_called_scripts resume="prepareTrade" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.TradesBlacklistUpdatedSignal'" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received trade blacklist update signal from subordinate %s for ware: %s. Scanning market: %s. Blacklist: %s.'.
          [this.assignedcontrolled.debugname, @event.param2.debugname, @event.param3.id, $scanningMarket, @player.entity.$pilotAcademyRAndRData.$tradesBlacklist]"
          chance="$debugchance" />
        <do_if value="$scanningMarket != true">
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <abort_called_scripts resume="prepareTrade" />
        </do_if>
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.TradeInfoDataRequest'" />
        <check_value value="@event.param2 != null" />
        <check_value value="this.assignedcontrolled.subordinates.indexof.{@event.param2} gt 0" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received trade info request from subordinate %s'.[this.assignedcontrolled.debugname, event.param2.debugname]"
          chance="$debugchance" />
        <do_if value="$tradeIsUnderPreparation">
          <debug_text text="'Pilot Academy [%s]: trade info is still under preparation, will not share info now'.[this.assignedcontrolled.debugname]"
            chance="$debugchance" />
        </do_if>
        <do_else>
          <set_value name="$shareInfoTarget" exact="event.param2" />
          <include_interrupt_actions ref="PilotAcademyShareTradeInfo" />
          <do_if value="$isShareInfoValid == false">
            <debug_text text="'Pilot Academy [%s]: trade info is not valid to share, will not share info with %s'.[this.assignedcontrolled.debugname, event.param2.debugname]"
              chance="$debugchance" />
            <set_value name="$tradeOperation" exact="''" />
            <abort_called_scripts resume="prepareTrade" />
          </do_if>
        </do_else>
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_player_subordinate_removed previous="this.assignedcontrolled" />
      </conditions>
      <actions>
        <include_interrupt_actions ref="PilotAcademyRemoveShipVariables" />
        <signal_objects object="player.entity" param="'AcademyPilotShipDeregisterRequest'" param2="event.object" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.WingDataIsUpdatedSignal'" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received data update notification' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
        <include_interrupt_actions ref="PilotAcademyProcessWingData" />
        <do_if value="$isMovingToTradeTarget == true and $tradeOperation == 'buy'">
          <debug_text
            text="'Pilot Academy [%s]: interrupted while moving to trade target %s for buying, going to reset trades.' .
              [this.assignedcontrolled.debugname, $tradeTarget.debugname]"
            chance="$debugchance" />
          <set_value name="$isMovingToTradeTarget" exact="false" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <abort_called_scripts resume="prepareTrade" />
        </do_if>
        <do_else>
          <set_value name="$shipInfo.$dataIsUpdated" exact="true" />
        </do_else>
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.DismissWingRequest'" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received dismiss wing order, finishing.' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
        <abort_called_scripts resume="finish" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.RemoveFromWingRequest'" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received remove from wing order, finishing.' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
        <abort_called_scripts resume="finish" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="player.entity" param="'PilotAcademyRAndR.TargetRankLevelChangedSignal'" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: received target rank level change notification' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
        <include_interrupt_actions ref="PilotAcademyGetTargetRankLevel" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.PrepareForPilotReplacementRequest'" />
        <check_value value="@event.param2 != null" />
      </conditions>
      <actions>
        <debug_text
          text="'Pilot Academy [%s]: preparing for pilot replacement by %s from %s.' . [this.assignedcontrolled.debugname, @event.param2.debugname, @event.param2.roleobject.debugname]"
          chance="$debugchance" />
        <set_value name="$replacementPilot" exact="@event.param2" />
        <set_value name="$tradeOperation" exact="''" />
        <abort_called_scripts resume="pilotReplacement" />
      </actions>
    </handler>
    <handler consume="false">
      <conditions>
        <event_object_signalled object="player.entity" param="'PilotAcademyRAndR.DebugLevelChangedSignal'" />
      </conditions>
      <actions>
        <debug_text
          text="'Pilot Academy [%s]: received debug level change notification' . [this.assignedcontrolled.debugname]"
          chance="$debugchance" />
        <include_interrupt_actions ref="PilotAcademyDebugApply" />
      </actions>
    </handler>
    <handler>
      <conditions>
        <event_game_loaded />
        <check_value value="@$scriptVersion == null or @$scriptVersion lt 5" />
      </conditions>
      <actions>
        <debug_text text="'Pilot Academy [%s]: game loaded, script version %s is old or not set' . [this.assignedcontrolled.debugname, @$scriptVersion]" chance="$debugchance" />
        <abort_called_scripts resume="abort" />
      </actions>
    </handler>
  </interrupts>
  <init>
    <set_value name="$isInit" exact="true" />
    <include_interrupt_actions ref="PilotAcademyDebugApply" />
    <debug_text
      text="'Pilot Academy [%s]: initialized order with wingId: %s, wingData: %s' .
        [this.assignedcontrolled.debugname, @$wingId, @$wingData]"
      chance="$debugchance" />
    <include_interrupt_actions ref="PilotAcademyProcessWingData" />
    <include_interrupt_actions ref="PilotAcademyInitShipVariables" />
    <set_value name="$enoughFactionsToCheck" exact="30" />
    <set_value name="$blacklistGroup" exact="blacklistgroup.civilian" />
    <set_value name="$tradeOfferTypes" exact="['buy', 'sell']" />
    <set_value name="$tradeRangeEnough" exact="10km" />
    <set_value name="$tradeVolumes" exact="[500000, 50000, 5000, 500, 50]" />
    <set_value name="$tradeOperation" exact="''" />
    <set_value name="$selectedFaction" exact="null" />
    <set_value name="$selectedBuyOffer" exact="null" />
    <set_value name="$selectedSellOffer" exact="null" />
    <set_value name="$targetCombinedSkill" exact="@$targetRankLevel * 20" />
    <set_value name="$tradeIsUnderPreparation" exact="false" />
    <set_value name="$isMovingToTradeTarget" exact="false" />
    <set_value name="$currentShip" exact="this.assignedcontrolled" />
    <set_value name="$rankUpReported" exact="false" />
    <set_value name="$lastTradeInfoRefresh" exact="0s" />
    <remove_value name="$isInit" />
    <set_value name="$shipDockSize" exact="this.assignedcontrolled.docksize" />
    <set_value name="$waitCoefficientBase" exact="5" />
    <set_value name="$scanningMarket" exact="false" />
    <set_value name="$scriptVersion" exact="5" />
    <set_value name="$lastUndockedStamp" exact="player.age" />
  </init>
  <patch sinceversion="2">
    <set_value name="$shipDockSize" exact="this.assignedcontrolled.docksize" />
  </patch>
  <patch sinceversion="3">
    <set_value name="$waitCoefficientBase" exact="5" />
  </patch>
  <patch sinceversion="5">
    <set_value name="$scanningMarket" exact="false" />
    <set_value name="$tradeWare" exact="nil" />
    <set_value name="$lastUndockedStamp" exact="player.age" />
  </patch>
  <attention min="unknown">
    <actions>

      <do_if value="$runMode == false">
        <debug_text text="'Pilot Academy [%s]: order executed outside an Academy run, terminating.' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
        <resume label="finish" />
      </do_if>

      <do_if value="this.assignedcontrolled.order" comment="Safety check in case the script is called from non-order script">
        <!-- Required for all infinite orders, no effect in case of finite timeout -->
        <set_order_syncpoint_reached order="this.assignedcontrolled.order" />
      </do_if>

      <label name="prepareTrade" />
      <include_interrupt_actions ref="PilotAcademyRecoverCurrentShipInfo" />

      <do_if value="this.assignedcontrolled.tradeorders.count">
        <debug_text
          text="'Pilot Academy [%s]: waiting for %s outstanding trade(s)'.[this.assignedcontrolled.debugname, this.assignedcontrolled.tradeorders.count]"
          chance="$debugchance" />
        <wait min="5s" max="8s" />
        <resume label="prepareTrade" />
      </do_if>
      <debug_text
        text="'Pilot Academy [%s]: started with: targetRankLevel %s, factionsNames count %s, selectedFaction %s, selectedBuyOffer %s, exists: %s,  selectedSellOffer %s, exists: %s'.
          [this.assignedcontrolled.debugname, $targetRankLevel, $factionNames.count, @$selectedFaction, @$selectedBuyOffer, @$selectedBuyOffer.exists, @$selectedSellOffer, @$selectedSellOffer.exists]"
        chance="$debugchance" />
      <do_if value="@$selectedFaction == null or @$selectedBuyOffer == null or $selectedBuyOffer.exists == false or @$selectedSellOffer == null or $selectedSellOffer.exists == false">

        <set_value name="$shipInfo.$dataIsUpdated" exact="false" />

        <do_if value="this.assignedcontrolled.commander != null">
          <debug_text
            text="'Pilot Academy [%s]: going to request parent %s for trade info' . [this.assignedcontrolled.debugname, this.assignedcontrolled.commander.debugname]"
            chance="$debugchance" />
          <resume label="waitInfoFromCommander" />
        </do_if>

        <set_value name="$tradeIsUnderPreparation" exact="true" />

        <set_value name="$scanningMarket" exact="true" />
        <set_command command="command.searchtrades" />

        <set_command_action commandaction="commandaction.searchingtrades" />

        <get_factions_by_relation faction="faction.player" relation="neutral" result="$factionsListAll" />
        <get_factions_by_relation faction="faction.player" relation="friend" result="$friendFactions" />
        <append_list_elements name="$factionsListAll" other="$friendFactions" />
        <get_factions_by_tag result="$hiddenFactions" tag="tag.hidden" />
        <remove_from_list name="$factionsListAll" list="$hiddenFactions" />
        <remove_from_list name="$factionsListAll" exact="faction.player" />
        <debug_text
          text="'Pilot Academy [%s]: evaluating %s factions for trade opportunities. Selected factions count: %s' . [this.assignedcontrolled.debugname, $factionsListAll.count, $factionNames.count]"
          chance="$debugchance" />

        <create_list name="$filteredFactions" />
        <do_for_each name="$faction" in="$factionsListAll" counter="$i">

          <do_if value="not ($faction.isactive and $faction.knowntoplayer) or $faction.isvisitor or @$faction.isrelationlocked">
            <debug_text
              text="'Pilot Academy [%s]: skipping faction %s due to inactive (%s) or unknown (%s) or visitor (%s) status or relation is locked (%s)' .
              [this.assignedcontrolled.debugname, @$faction.name, not @$faction.isactive, not @$faction.knowntoplayer, @$faction.isvisitor, @$faction.isrelationlocked]"
              chance="$debugchance" />
            <continue />
          </do_if>

          <do_if value="$faction.hasrelation.dock.{faction.player} == false">
            <debug_text text="'Pilot Academy [%s]: skipping faction %s due to no docking rights' . [this.assignedcontrolled.debugname, @$faction.name]"
              chance="$debugchance" />
            <continue />
          </do_if>

          <debug_text
            text="'Pilot Academy [%s]: faction Names count %s, evaluating faction id %s presence %s with relation %s' .
              [this.assignedcontrolled.debugname, @$factionNames.count, @$faction.id, @$factionNames.indexof.{$faction.id}, @$faction.relationto.{faction.player}]"
            chance="$debugchance" />

          <do_if value="@$factionNames.count == 0">
            <append_to_list name="$filteredFactions" exact="$faction" />
          </do_if>
          <do_elseif value="$factionNames.indexof.{$faction.id} gt 0">
            <append_to_list name="$filteredFactions" exact="$faction" />
          </do_elseif>

          <do_if value="$i % $waitCoefficientBase == 0">
            <wait min="1ms" max="3ms" />
          </do_if>

        </do_for_each>

        <sort_list list="$filteredFactions" sortbyvalue="loop.element.relationto.{faction.player}" sortdescending="false" />

        <debug_text
          text="'Pilot Academy [%s]: filtered factions count %s after filters to be processed for Primary Goal %s' .
          [this.assignedcontrolled.debugname, $filteredFactions.count, $primaryGoal]"
          chance="$debugchance" />

        <set_value name="$bestDistance" exact="-1" />
        <set_value name="$bestGateDistance" exact="999" />

        <set_value name="$tradeFound" exact="false" />
        <do_if value="$primaryGoal == 'rank'">
          <do_for_each name="$tradeVolumeMin" in="$tradeVolumes">
            <debug_text
              text="'Pilot Academy [%s]: evaluating trade volume tier %s on factions count %s' . [this.assignedcontrolled.debugname, $tradeVolumeMin, $filteredFactions.count]"
              chance="$debugchance" />
            <do_for_each name="$faction" in="$filteredFactions">
              <get_wares_illegal_to result="$illegalWares" faction="$faction" />
              <debug_text
                text="'Pilot Academy [%s]: evaluating faction %s with relation %s and illegal wares count: %s' . [this.assignedcontrolled.debugname, @$faction.name, @$faction.relationto.{faction.player}, $illegalWares.count]"
                chance="$debugchance" />

              <do_for_each name="$offerType" in="$tradeOfferTypes">

                <include_interrupt_actions ref="PilotAcademySearchForDemandOffers" />

                <do_if value="$demandOffers.count gt 0">

                  <sort_list list="$demandOffers" sortbyvalue="loop.element.amount" sortdescending="true" />

                  <do_for_each name="$demandOffer" in="$demandOffers" counter="$j">

                    <debug_text
                      text="'Pilot Academy [%s]: evaluating %s offer for %s of %s with volume %s from %s. Price %s, Quantity factor %s. Is illegal: %s' .
                      [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $demandOffer.volume, $demandOffer.owner.debugname, $demandOffer.unitprice.formatted.default, $demandOffer.quantityfactor, $illegalWares.indexof.{$demandOffer.ware} gt 0]"
                      chance="$debugchance" />

                    <do_if value="$demandOffer.ware.waretransport != waretransport.container">
                      <continue />
                    </do_if>

                    <do_if value="this.assignedcontrolled.gatedistance.{$demandOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
                      <debug_text text="'Pilot Academy [%s]: no path possible to: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <do_if value="@$demandOffer.owner.dockingenabled != true or @$demandOffer.owner.dockingallowed.{this.assignedcontrolled} != true">
                      <debug_text text="'Pilot Academy [%s]: no docking possible to: %s (enabled: %s, allowed: %s), skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.debugname, @$demandOffer.owner.dockingenabled, @$demandOffer.owner.dockingallowed.{this.assignedcontrolled}]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <!-- <debug_text text="'Pilot Academy [%s]: dock sizes (S: %s, isdockingallowed: %s, dockstate: %s, todockpos: %s, M: %s, isdockingallowed: %s, dockstate: %s, todockpos: %s) available at: %s.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.dock.{$shipDockSize}, @$demandOffer.owner.dock.{$shipDockSize}.isdockingallowed, @$demandOffer.owner.dock.{$shipDockSize}.dockstate, @$demandOffer.owner.dock.{$shipDockSize}.todockpos, @$demandOffer.owner.dock.{tag.dock_m}, @$demandOffer.owner.dock.{tag.dock_m}.isdockingallowed, @$demandOffer.owner.dock.{tag.dock_m}.dockstate, @$demandOffer.owner.dock.{tag.dock_m}.todockpos, @$demandOffer.owner.debugname]"
                      chance="$debugchance" /> -->
                    <do_if value="@$demandOffer.owner.dock.{$shipDockSize} == null or @$demandOffer.owner.dock.{$shipDockSize}.isdockingallowed != true or @$demandOffer.owner.dock.{$shipDockSize}.todockpos == null">
                      <debug_text text="'Pilot Academy [%s]: no docks of right size %s, or no docking allowed: %s, or no dock position %s at: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.dock.{$shipDockSize}, @$demandOffer.owner.dock.{$shipDockSize}.isdockingallowed, @$demandOffer.owner.dock.{$shipDockSize}.todockpos, @$demandOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <do_if value="$illegalWares.indexof.{$demandOffer.ware} gt 0">
                      <debug_text
                        text="'Pilot Academy [%s]: skipping %s offer for %s of %s as ware is illegal for faction %s' .
                        [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $faction.name]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$stationForTrade" exact="$demandOffer.owner" />
                    <include_interrupt_actions ref="PilotAcademyGetIllegalWaresForStation" />
                    <do_if value="$illegalWaresForStation.indexof.{$demandOffer.ware} gt 0">
                      <debug_text
                        text="'Pilot Academy [%s]: skipping %s offer for %s of %s as ware is illegal for station %s and sector %s' .
                        [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $stationForTrade.debugname, $stationForTrade.sector.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$blackListCheckOperation" exact="$offerType" />
                    <set_value name="$blackListCheckOffer" exact="$demandOffer" />
                    <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
                    <do_if value="$isBlackListed">
                      <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                        [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$demandAmount" exact="$demandOffer.amount" />

                    <set_value name="$demandOwner" exact="$demandOffer.owner" />

                    <include_interrupt_actions ref="PilotAcademySearchForSupplyOffers" />

                    <do_if value="$supplyOffers.count == 0">
                      <debug_text
                        text="'Pilot Academy [%s]: no opposite offers to %s of %s, skipping' . [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <sort_list list="$supplyOffers"
                      sortbyvalue="(if $demandOwner.gatedistance.{loop.element.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0 then 999 else $demandOwner.gatedistance.{loop.element.owner}.{$blacklistGroup}.{this.assignedcontrolled})
                      * (if $demandOffer.owner.trueowner.id == loop.element.owner.trueowner.id then 1 else 2)"
                      sortdescending="false" />

                    <do_for_each name="$supplyOffer" in="$supplyOffers" counter="$k">

                      <do_if value="this.assignedcontrolled.gatedistance.{$supplyOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
                        <debug_text text="'Pilot Academy [%s]: no path possible to: %s, skipping.'.
                          [@this.assignedcontrolled.debugname, @$supplyOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.dockingenabled != true or @$supplyOffer.owner.dockingallowed.{this.assignedcontrolled} != true">
                        <debug_text text="'Pilot Academy [%s]: no docking possible to: %s (enabled: %s, allowed: %s), skipping.'.
                        [@this.assignedcontrolled.debugname, @$supplyOffer.owner.debugname, @$supplyOffer.owner.dockingenabled, @$supplyOffer.owner.dockingallowed.{this.assignedcontrolled}]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.dock.{$shipDockSize} == null or @$supplyOffer.owner.dock.{$shipDockSize}.isdockingallowed != true or @$supplyOffer.owner.dock.{$shipDockSize}.todockpos == null">
                        <debug_text text="'Pilot Academy [%s]: no docks of right size %s, or no docking allowed: %s, or no dock position %s at: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$supplyOffer.owner.dock.{$shipDockSize}, @$supplyOffer.owner.dock.{$shipDockSize}.isdockingallowed, @$supplyOffer.owner.dock.{$shipDockSize}.todockpos, @$supplyOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$stationForTrade" exact="$supplyOffer.owner" />
                      <include_interrupt_actions ref="PilotAcademyGetIllegalWaresForStation" />
                      <do_if value="$illegalWaresForStation.indexof.{$supplyOffer.ware} gt 0">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping opposite offer for %s as ware is illegal for station %s in sector %s' .
                          [this.assignedcontrolled.debugname, $supplyOffer.ware.id, $stationForTrade.debugname, $stationForTrade.sector.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$blackListCheckOperation" exact="if $offerType == 'sell' then 'buy' else 'sell'" />
                      <set_value name="$blackListCheckOffer" exact="$supplyOffer" />
                      <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
                      <do_if value="$isBlackListed">
                        <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                        [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.isrelationlocked">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping opposite offer for %s from %s as relation with %s is locked' .
                          [this.assignedcontrolled.debugname, $supplyOffer.ware.id, @$supplyOffer.owner.trueowner.name, @$supplyOffer.owner.isrelationlocked]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$distanceCoefficient" exact="if $demandOffer.owner.trueowner.id == $supplyOffer.owner.trueowner.id then 1 else 2" />
                      <set_value name="$gateDistance"
                        exact="$demandOwner.gatedistance.{$supplyOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} * $distanceCoefficient" />

                      <do_if value="$gateDistance lt 0">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping all next opposite offers to %s of %s as no gate connection' . [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                          chance="$debugchance" />
                        <break />
                      </do_if>

                      <set_value name="$tradeDistance" exact="$demandOwner.distanceto.{$supplyOffer.owner}* $distanceCoefficient" />
                      <debug_text
                        text="'Pilot Academy [%s]: gate distance for opposite offer for %s with volume %s from %s to %s of %s offer on %s is %s, best %s. Distance: %s, best %s. Distance coefficient: %s. Price %s, Quantity factor %s' .
                        [this.assignedcontrolled.debugname, $supplyOffer.amount, $supplyOffer.volume, $supplyOffer.owner.debugname, $offerType, $demandOffer.ware.id, $demandOwner.debugname, $gateDistance, $bestGateDistance, $tradeDistance, $bestDistance, $distanceCoefficient, $supplyOffer.unitprice.formatted.default, $supplyOffer.quantityfactor]"
                        chance="$debugchance" />
                      <do_if value="$bestGateDistance lt $gateDistance">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping all next opposite offers to %s due to worse gate distance %s vs best %s' .
                        [this.assignedcontrolled.debugname, $offerType, $gateDistance, $bestGateDistance]"
                          chance="$debugchance" />
                        <break />
                      </do_if>

                      <set_value name="$supplyAmount" exact="$supplyOffer.amount" />


                      <do_if
                        value="$bestDistance == -1 or $bestGateDistance gt $gateDistance or $bestGateDistance == $gateDistance and $bestDistance gt $tradeDistance ">
                        <set_value name="$bestGateDistance" exact="$gateDistance" />
                        <set_value name="$bestDistance" exact="$tradeDistance" />
                        <set_value name="$selectedSellOffer" exact="if $offerType == 'buy' then $supplyOffer else $demandOffer" />
                        <set_value name="$selectedBuyOffer" exact="if $offerType == 'sell' then $supplyOffer else $demandOffer" />
                        <set_value name="$selectedFaction" exact="$faction" />
                        <debug_text
                          text="'Pilot Academy [%s]: new best pair of offers of %s found from %s to %s with distance %s, coefficient %s' .
                          [this.assignedcontrolled.debugname, $demandOwner.debugname, $demandOffer.ware.id, $supplyOffer.owner.debugname, $bestDistance, $distanceCoefficient]"
                          chance="$debugchance" />
                        <do_if value="$bestGateDistance == 0 and $bestDistance le $tradeRangeEnough">
                          <debug_text
                            text="'Pilot Academy [%s]: short enough distance %s (enough %s) and gate distance %s, stopping search for better trades'.
                          [this.assignedcontrolled.debugname, $bestDistance, $tradeRangeEnough, $bestGateDistance]"
                            chance="$debugchance" />
                          <set_value name="$tradeFound" exact="true" />
                        </do_if>
                      </do_if>

                      <remove_value name="$tradeDistance" />
                      <remove_value name="$gateDistance" />
                      <do_if value="$tradeFound">
                        <debug_text
                          text="'Pilot Academy [%s]: trade found, stopping evaluation of further opposite offers %s of %s' .
                          [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                          chance="$debugchance" />
                        <break />
                      </do_if>
                      <do_if value="$k % $waitCoefficientBase == 0">
                        <wait min="1ms" max="3ms" />
                      </do_if>
                    </do_for_each>

                    <remove_value name="$supplyOffers" />
                    <remove_value name="$supplyAmount" />
                    <do_if value="$tradeFound">
                      <debug_text
                        text="'Pilot Academy [%s]: trade found, stopping evaluation of further offers %s of %s' .
                      [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                        chance="$debugchance" />
                      <break />
                    </do_if>
                    <do_if value="$j % $waitCoefficientBase == 0">
                      <wait min="1ms" max="3ms" />
                    </do_if>
                  </do_for_each>

                </do_if>
                <remove_value name="$demandOffers" />
                <remove_value name="$demandOwner" />
                <remove_value name="$demandAmount" />
                <do_if value="$tradeFound">
                  <debug_text
                    text="'Pilot Academy [%s]: trade found, stopping evaluation of further offer types for faction %s' . [this.assignedcontrolled.debugname, @$faction.name]"
                    chance="$debugchance" />
                  <break />
                </do_if>
              </do_for_each>

              <do_if value="@$selectedFaction != null">
                <do_if value="$selectedSellOffer.owner.trueowner.id != $selectedBuyOffer.owner.trueowner.id">
                  <set_value name="$bestGateDistance" exact="$bestGateDistance / 2" />
                  <set_value name="$bestDistance" exact="$bestDistance / 2" />
                </do_if>
                <debug_text
                  text="'Pilot Academy [%s]: **Best offers pair** current for faction %s: sell ware %s from %s in amount %s and volume %s, price %s, quantity factor %s, buy on %s with amount %s and volume %s, price %s, quantity factor %s with distance %s and gate distance %s'
                  .[this.assignedcontrolled.debugname, $selectedFaction, $selectedSellOffer.ware.id, $selectedSellOffer.owner.debugname, $selectedSellOffer.amount, $selectedSellOffer.volume, $selectedSellOffer.unitprice.formatted.default, $selectedSellOffer.quantityfactor, $selectedBuyOffer.owner.debugname, $selectedBuyOffer.amount, $selectedBuyOffer.volume, $selectedBuyOffer.unitprice.formatted.default, $selectedBuyOffer.quantityfactor, $bestDistance, $bestGateDistance]"
                  chance="$debugchance" />
              </do_if>
              <do_else>
                <debug_text text="'Pilot Academy [%s]: no qualifying trades found for faction %s' .[this.assignedcontrolled.debugname, @$faction.name]"
                  chance="$debugchance" />
              </do_else>

              <do_if value="$tradeFound">
                <debug_text text="'Pilot Academy [%s]: trade found, stopping evaluation of further factions'.[this.assignedcontrolled.debugname]"
                  chance="$debugchance" />
                <break />
              </do_if>
              <wait min="1ms" max="3ms" />
            </do_for_each>
            <do_if value="$tradeFound">
              <debug_text text="'Pilot Academy [%s]: trade found, stopping evaluation of further volumes and factions' .[this.assignedcontrolled.debugname]"
                chance="$debugchance" />
              <break />
            </do_if>
          </do_for_each>
        </do_if>
        <do_else>
          <do_for_each name="$faction" in="$filteredFactions">
            <get_wares_illegal_to result="$illegalWares" faction="$faction" />
            <debug_text
              text="'Pilot Academy [%s]: evaluating faction %s with relation %s and illegal wares count: %s' . [this.assignedcontrolled.debugname, @$faction.name, @$faction.relationto.{faction.player}, $illegalWares.count]"
              chance="$debugchance" />
            <do_for_each name="$tradeVolumeMin" in="$tradeVolumes">
              <debug_text
                text="'Pilot Academy [%s]: evaluating trade volume tier %ss' . [this.assignedcontrolled.debugname, $tradeVolumeMin]"
                chance="$debugchance" />

              <do_for_each name="$offerType" in="$tradeOfferTypes">

                <include_interrupt_actions ref="PilotAcademySearchForDemandOffers" />

                <do_if value="$demandOffers.count gt 0">

                  <sort_list list="$demandOffers" sortbyvalue="loop.element.amount" sortdescending="true" />

                  <do_for_each name="$demandOffer" in="$demandOffers" counter="$j">

                    <debug_text
                      text="'Pilot Academy [%s]: evaluating %s offer for %s of %s with volume %s from %s. Price %s, Quantity factor %s. Is illegal: %s' .
                      [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $demandOffer.volume, $demandOffer.owner.debugname, $demandOffer.unitprice.formatted.default, $demandOffer.quantityfactor, $illegalWares.indexof.{$demandOffer.ware} gt 0]"
                      chance="$debugchance" />

                    <do_if value="$demandOffer.ware.waretransport != waretransport.container">
                      <continue />
                    </do_if>

                    <do_if value="this.assignedcontrolled.gatedistance.{$demandOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
                      <debug_text text="'Pilot Academy [%s]: no path possible to: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <do_if value="@$demandOffer.owner.dockingenabled != true or @$demandOffer.owner.dockingallowed.{this.assignedcontrolled} != true">
                      <debug_text text="'Pilot Academy [%s]: no docking possible to: %s (enabled: %s, allowed: %s), skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.debugname, @$demandOffer.owner.dockingenabled, @$demandOffer.owner.dockingallowed.{this.assignedcontrolled}]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <do_if value="@$demandOffer.owner.dock.{$shipDockSize} == null or @$demandOffer.owner.dock.{$shipDockSize}.isdockingallowed != true or @$demandOffer.owner.dock.{$shipDockSize}.todockpos == null">
                      <debug_text text="'Pilot Academy [%s]: no docks of right size %s, or no docking allowed: %s, or no dock position %s at: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$demandOffer.owner.dock.{$shipDockSize}, @$demandOffer.owner.dock.{$shipDockSize}.isdockingallowed, @$demandOffer.owner.dock.{$shipDockSize}.todockpos, @$demandOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <do_if value="$illegalWares.indexof.{$demandOffer.ware} gt 0">
                      <debug_text
                        text="'Pilot Academy [%s]: skipping %s offer for %s of %s as ware is illegal for faction %s' .
                        [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $faction.name]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$stationForTrade" exact="$demandOffer.owner" />
                    <include_interrupt_actions ref="PilotAcademyGetIllegalWaresForStation" />
                    <do_if value="$illegalWaresForStation.indexof.{$demandOffer.ware} gt 0">
                      <debug_text
                        text="'Pilot Academy [%s]: skipping %s offer for %s of %s as ware is illegal for station %s and sector %s' .
                        [this.assignedcontrolled.debugname, $offerType, $demandOffer.amount, $demandOffer.ware.id, $stationForTrade.debugname, $stationForTrade.sector.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$blackListCheckOperation" exact="$offerType" />
                    <set_value name="$blackListCheckOffer" exact="$demandOffer" />
                    <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
                    <do_if value="$isBlackListed">
                      <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                        [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <set_value name="$demandAmount" exact="$demandOffer.amount" />

                    <set_value name="$demandOwner" exact="$demandOffer.owner" />

                    <include_interrupt_actions ref="PilotAcademySearchForSupplyOffers" />

                    <do_if value="$supplyOffers.count == 0">
                      <debug_text
                        text="'Pilot Academy [%s]: no opposite offers to %s of %s, skipping' . [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                        chance="$debugchance" />
                      <continue />
                    </do_if>

                    <sort_list list="$supplyOffers"
                      sortbyvalue="(if $demandOwner.gatedistance.{loop.element.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0 then 999 else $demandOwner.gatedistance.{loop.element.owner}.{$blacklistGroup}.{this.assignedcontrolled})
                      * (if $demandOffer.owner.trueowner.id == loop.element.owner.trueowner.id then 1 else 2)"
                      sortdescending="false" />

                    <do_for_each name="$supplyOffer" in="$supplyOffers" counter="$k">

                      <do_if value="this.assignedcontrolled.gatedistance.{$supplyOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
                        <debug_text text="'Pilot Academy [%s]: no path possible to: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$supplyOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.dockingenabled != true or @$supplyOffer.owner.dockingallowed.{this.assignedcontrolled} != true">
                        <debug_text text="'Pilot Academy [%s]: no docking possible to: %s (enabled: %s, allowed: %s), skipping.'.
                        [@this.assignedcontrolled.debugname, @$supplyOffer.owner.debugname, @$supplyOffer.owner.dockingenabled, @$supplyOffer.owner.dockingallowed.{this.assignedcontrolled}]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.dock.{$shipDockSize} == null or @$supplyOffer.owner.dock.{$shipDockSize}.isdockingallowed != true or @$supplyOffer.owner.dock.{$shipDockSize}.todockpos == null">
                        <debug_text text="'Pilot Academy [%s]: no docks of right size %s, or no docking allowed: %s, or no dock position %s at: %s, skipping.'.
                        [@this.assignedcontrolled.debugname, @$supplyOffer.owner.dock.{$shipDockSize}, @$supplyOffer.owner.dock.{$shipDockSize}.isdockingallowed, @$supplyOffer.owner.dock.{$shipDockSize}.todockpos, @$supplyOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$stationForTrade" exact="$supplyOffer.owner" />
                      <include_interrupt_actions ref="PilotAcademyGetIllegalWaresForStation" />
                      <do_if value="$illegalWaresForStation.indexof.{$supplyOffer.ware} gt 0">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping opposite offer for %s as ware is illegal for station %s in sector %s' .
                          [this.assignedcontrolled.debugname, $supplyOffer.ware.id, $stationForTrade.debugname, $stationForTrade.sector.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$blackListCheckOperation" exact="if $offerType == 'sell' then 'buy' else 'sell'" />
                      <set_value name="$blackListCheckOffer" exact="$supplyOffer" />
                      <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
                      <do_if value="$isBlackListed">
                        <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                        [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <do_if value="@$supplyOffer.owner.isrelationlocked">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping opposite offer for %s from %s as relation with %s is locked' .
                          [this.assignedcontrolled.debugname, $supplyOffer.ware.id, @$supplyOffer.owner.trueowner.name, @$supplyOffer.owner.isrelationlocked]"
                          chance="$debugchance" />
                        <continue />
                      </do_if>

                      <set_value name="$distanceCoefficient" exact="if $demandOffer.owner.trueowner.id == $supplyOffer.owner.trueowner.id then 1 else 2" />
                      <set_value name="$gateDistance"
                        exact="$demandOwner.gatedistance.{$supplyOffer.owner}.{$blacklistGroup}.{this.assignedcontrolled} * $distanceCoefficient" />

                      <do_if value="$gateDistance lt 0">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping all next opposite offers to %s of %s as no gate connection' . [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                          chance="$debugchance" />
                        <break />
                      </do_if>

                      <set_value name="$tradeDistance" exact="$demandOwner.distanceto.{$supplyOffer.owner}* $distanceCoefficient" />
                      <debug_text
                        text="'Pilot Academy [%s]: gate distance for opposite offer for %s with volume %s from %s to %s of %s offer on %s is %s, best %s. Distance: %s, best %s. Distance coefficient: %s. Price %s, Quantity factor %s' .
                        [this.assignedcontrolled.debugname, $supplyOffer.amount, $supplyOffer.volume, $supplyOffer.owner.debugname, $offerType, $demandOffer.ware.id, $demandOwner.debugname, $gateDistance, $bestGateDistance, $tradeDistance, $bestDistance, $distanceCoefficient, $supplyOffer.unitprice.formatted.default, $supplyOffer.quantityfactor]"
                        chance="$debugchance" />
                      <do_if value="$bestGateDistance lt $gateDistance">
                        <debug_text
                          text="'Pilot Academy [%s]: skipping all next opposite offers to %s due to worse gate distance %s vs best %s' .
                        [this.assignedcontrolled.debugname, $offerType, $gateDistance, $bestGateDistance]"
                          chance="$debugchance" />
                        <break />
                      </do_if>

                      <set_value name="$supplyAmount" exact="$supplyOffer.amount" />


                      <do_if
                        value="$bestDistance == -1 or $bestGateDistance gt $gateDistance or $bestGateDistance == $gateDistance and $bestDistance gt $tradeDistance ">
                        <set_value name="$bestGateDistance" exact="$gateDistance" />
                        <set_value name="$bestDistance" exact="$tradeDistance" />
                        <set_value name="$selectedSellOffer" exact="if $offerType == 'buy' then $supplyOffer else $demandOffer" />
                        <set_value name="$selectedBuyOffer" exact="if $offerType == 'sell' then $supplyOffer else $demandOffer" />
                        <set_value name="$selectedFaction" exact="$faction" />
                        <debug_text
                          text="'Pilot Academy [%s]: new best pair of offers of %s found from %s to %s with distance %s, coefficient %s' .
                          [this.assignedcontrolled.debugname, $demandOwner.debugname, $demandOffer.ware.id, $supplyOffer.owner.debugname, $bestDistance, $distanceCoefficient]"
                          chance="$debugchance" />
                        <do_if value="$bestGateDistance == 0 and $bestDistance le $tradeRangeEnough">
                          <debug_text
                            text="'Pilot Academy [%s]: short enough distance %s (enough %s) and gate distance %s, stopping search for better trades'.
                          [this.assignedcontrolled.debugname, $bestDistance, $tradeRangeEnough, $bestGateDistance]"
                            chance="$debugchance" />
                          <set_value name="$tradeFound" exact="true" />
                        </do_if>
                      </do_if>

                      <remove_value name="$tradeDistance" />
                      <remove_value name="$gateDistance" />
                      <do_if value="$tradeFound">
                        <debug_text
                          text="'Pilot Academy [%s]: trade found, stopping evaluation of further opposite offers %s of %s' .
                          [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                          chance="$debugchance" />
                        <break />
                      </do_if>
                      <do_if value="$k % $waitCoefficientBase == 0">
                        <wait min="1ms" max="3ms" />
                      </do_if>
                    </do_for_each>

                    <remove_value name="$supplyOffers" />
                    <remove_value name="$supplyAmount" />
                    <do_if value="$tradeFound">
                      <debug_text
                        text="'Pilot Academy [%s]: trade found, stopping evaluation of further offers %s of %s' .
                      [this.assignedcontrolled.debugname, $offerType, $demandOffer.ware.id]"
                        chance="$debugchance" />
                      <break />
                    </do_if>
                    <do_if value="$j % $waitCoefficientBase == 0">
                      <wait min="1ms" max="3ms" />
                    </do_if>
                  </do_for_each>

                </do_if>
                <remove_value name="$demandOffers" />
                <remove_value name="$demandOwner" />
                <remove_value name="$demandAmount" />
                <do_if value="$tradeFound">
                  <debug_text
                    text="'Pilot Academy [%s]: trade found, stopping evaluation of further offer types for faction %s' . [this.assignedcontrolled.debugname, @$faction.name]"
                    chance="$debugchance" />
                  <break />
                </do_if>
              </do_for_each>

              <do_if value="@$selectedFaction != null">
                <do_if value="$selectedSellOffer.owner.trueowner.id != $selectedBuyOffer.owner.trueowner.id">
                  <set_value name="$bestGateDistance" exact="$bestGateDistance / 2" />
                  <set_value name="$bestDistance" exact="$bestDistance / 2" />
                </do_if>
                <debug_text
                  text="'Pilot Academy [%s]: **Best offers pair** current for faction %s: sell ware %s from %s in amount %s and volume %s, price %s, quantity factor %s, buy on %s with amount %s and volume %s, price %s, quantity factor %s with distance %s and gate distance %s'
                  .[this.assignedcontrolled.debugname, $selectedFaction, $selectedSellOffer.ware.id, $selectedSellOffer.owner.debugname, $selectedSellOffer.amount, $selectedSellOffer.volume, $selectedSellOffer.unitprice.formatted.default, $selectedSellOffer.quantityfactor, $selectedBuyOffer.owner.debugname, $selectedBuyOffer.amount, $selectedBuyOffer.volume, $selectedBuyOffer.unitprice.formatted.default, $selectedBuyOffer.quantityfactor, $bestDistance, $bestGateDistance]"
                  chance="$debugchance" />
              </do_if>
              <do_else>
                <debug_text text="'Pilot Academy [%s]: no qualifying trades found for faction %s' .[this.assignedcontrolled.debugname, @$faction.name]"
                  chance="$debugchance" />
              </do_else>

              <do_if value="$tradeFound">
                <debug_text text="'Pilot Academy [%s]: trade found, stopping evaluation of further volumes'.[this.assignedcontrolled.debugname]"
                  chance="$debugchance" />
                <break />
              </do_if>
              <wait min="1ms" max="3ms" />
            </do_for_each>
            <do_if value="@$selectedFaction != null and @$selectedBuyOffer != null and @$selectedSellOffer != null">
              <set_value name="$tradeFound" exact="true" />
            </do_if>
            <do_if value="$tradeFound">
              <debug_text text="'Pilot Academy [%s]: trade found, stopping evaluation of further factions' .[this.assignedcontrolled.debugname]"
                chance="$debugchance" />
              <break />
            </do_if>
          </do_for_each>
        </do_else>
        <do_if value="@$selectedFaction != null and @$selectedBuyOffer != null and @$selectedSellOffer != null">
          <set_value name="$tradeFound" exact="true" />
          <set_value name="$lastTradeInfoRefresh" exact="player.age" />
        </do_if>
        <do_if value="not $tradeFound">
          <debug_text text="'Pilot Academy [%s]: no qualifying trade found after evaluating all factions' .[this.assignedcontrolled.debugname]"
            chance="$debugchance" />
          <wait min="6s" max="10s" />
          <resume label="prepareTrade" />
        </do_if>
        <do_if value="$selectedSellOffer.owner == $selectedBuyOffer.owner">
          <debug_text
            text="'Pilot Academy [%s]: selected buy and sell offers have the same owner %s, going to find a ware with lowest price' .
              [this.assignedcontrolled.debugname, $selectedBuyOffer.owner.debugname]"
            chance="$debugchance" />
          <find_sell_offer tradepartner="this.assignedcontrolled" seller="$selectedSellOffer.owner" result="$localSellOffers" multiple="true">
            <amount min="$selectedSellOffer.amount / 2" />
          </find_sell_offer>
          <set_value name="$stationForTrade" exact="$selectedSellOffer.owner" />
          <include_interrupt_actions ref="PilotAcademyGetIllegalWaresForStation" />
          <sort_list list="$localSellOffers" sortbyvalue="loop.element.unitprice" sortdescending="false" />
          <do_for_each name="$localSellOffer" in="$localSellOffers" counter="$i">
            <do_if value="$localSellOffer.ware.id == $selectedSellOffer.ware.id">
              <debug_text
                text="'Pilot Academy [%s]: skipping wares %s equal to existing one, and break checks as it would not improve the trade' .
                [this.assignedcontrolled.debugname, $localSellOffer.ware.id]"
                chance="$debugchance" />
              <break />
            </do_if>
            <do_if value="$illegalWaresForStation.indexof.{$localSellOffer.ware} gt 0">
              <debug_text
                text="'Pilot Academy [%s]: skipping local sell offer for %s of %s as ware is illegal for faction %s' .
                [this.assignedcontrolled.debugname, $offerType, $localSellOffer.amount, $localSellOffer.ware.id, $selectedSellOffer.owner.faction.name]"
                chance="$debugchance" />
              <continue />
            </do_if>

            <set_value name="$blackListCheckOperation" exact="'sell'" />
            <set_value name="$blackListCheckOffer" exact="$localSellOffer" />
            <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
            <do_if value="$isBlackListed">
              <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                        [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                chance="$debugchance" />
              <continue />
            </do_if>

            <debug_text
              text="'Pilot Academy [%s]: evaluating local sell offer for ware %s with amount %s and volume %s, price %s, quantity factor %s' .
                [this.assignedcontrolled.debugname, $localSellOffer.ware.id, $localSellOffer.amount, $localSellOffer.volume, $localSellOffer.unitprice.formatted.default, $localSellOffer.quantityfactor]"
              chance="$debugchance" />
            <find_buy_offer result="$matchingBuyOffers" tradepartner="this.assignedcontrolled" buyer="$selectedBuyOffer.owner" wares="$localSellOffer.ware"
              multiple="true">
              <amount min="$selectedBuyOffer.amount / 2" />
            </find_buy_offer>
            <do_if value="$matchingBuyOffers.count gt 0">

              <set_value name="$blackListCheckOperation" exact="'buy'" />
              <set_value name="$blackListCheckOffer" exact="$matchingBuyOffers.{1}" />
              <include_interrupt_actions ref="PilotAcademyCheckTradesBlacklist" />
              <do_if value="$isBlackListed">
                <debug_text text="'Pilot Academy [%s]: skipping %s offer %s on %s as trade is blacklisted' .
                    [this.assignedcontrolled.debugname, $blackListCheckOperation, $blackListCheckOffer.ware.id, $blackListCheckOffer.owner.debugname]"
                  chance="$debugchance" />
                <continue />
              </do_if>

              <set_value name="$selectedSellOffer" exact="$localSellOffer" />
              <set_value name="$selectedBuyOffer" exact="$matchingBuyOffers.{1}" />
              <debug_text
                text="'Pilot Academy [%s]: **Best offers pair**  new for faction %s: sell ware %s from %s in amount %s and volume %s, price %s, quantity factor %s, buy on %s with amount %s and volume %s, price %s, quantity factor %s with distance %s and gate distance %s'
                  .[this.assignedcontrolled.debugname, $selectedFaction, $selectedSellOffer.ware.id, $selectedSellOffer.owner.debugname, $selectedSellOffer.amount, $selectedSellOffer.volume, $selectedSellOffer.unitprice.formatted.default, $selectedSellOffer.quantityfactor, $selectedBuyOffer.owner.debugname, $selectedBuyOffer.amount, $selectedBuyOffer.volume, $selectedBuyOffer.unitprice.formatted.default, $selectedBuyOffer.quantityfactor, $bestDistance, $bestGateDistance]"
                chance="$debugchance" />
            </do_if>
            <do_if value="$i % $waitCoefficientBase == 0">
              <wait min="1ms" max="3ms" />
            </do_if>
          </do_for_each>
          <remove_value name="$localSellOffers" />
          <remove_value name="$matchingBuyOffers" />
        </do_if>
        <set_value name="$tradeIsUnderPreparation" exact="false" />
        <do_for_each name="$subordinate" in="this.assignedcontrolled.subordinates">
          <debug_text text="'Pilot Academy [%s]: going to share trade info with subordinate %s'.[this.assignedcontrolled.debugname, $subordinate.debugname]"
            chance="$debugchance" />
          <signal_objects object="$subordinate" param="'PilotAcademyRAndR.WingDataIsUpdatedSignal'" />
        </do_for_each>
      </do_if>
      <debug_text
        text="'Pilot Academy [%s]: --- **Selected trade** from %s: sell ware %s from %s in amount %s and volume %s, price %s, quantity factor %s, buy on %s with amount %s and volume %s, price %s, quantity factor %s.'
          .[this.assignedcontrolled.debugname, $selectedFaction, $selectedSellOffer.ware.id, $selectedSellOffer.owner.debugname, $selectedSellOffer.amount, $selectedSellOffer.volume, $selectedSellOffer.unitprice.formatted.default, $selectedSellOffer.quantityfactor, $selectedBuyOffer.owner.debugname, $selectedBuyOffer.amount, $selectedBuyOffer.volume, $selectedBuyOffer.unitprice.formatted.default, $selectedBuyOffer.quantityfactor]"
        chance="$debugchance" />

      <set_value name="$tradeWare" exact="$selectedSellOffer.ware" />

      <label name="proceedWithTrade" />
      <set_value name="$scanningMarket" exact="false" />

      <set_value name="$seller" exact="$selectedSellOffer.owner" />
      <set_value name="$buyer" exact="$selectedBuyOffer.owner" />

      <set_value name="$tradeAmountDefault" exact="1" />

      <include_interrupt_actions ref="PilotAcademyCheckAndClearReservation" />

      <do_if value="$tradeOperation == 'buy'">
        <debug_text
          text="'Pilot Academy [%s]: completed buying %s of %s from %s' . [this.assignedcontrolled.debugname, $tradeAmountDefault, $tradeWare.id, $seller.debugname]"
          chance="$debugchance" />
        <set_value name="$tradeTarget" exact="$buyer" />
        <set_value name="$targetType" exact="'buyer'" />
        <set_value name="$tradeOperation" exact="'sell'" />
        <set_value name="$tradeOffer" exact="$selectedBuyOffer" />
        <wait min="50ms" max="100ms" />
        <do_if value="this.assignedcontrolled.gatedistance.{$tradeTarget}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
          <debug_text text="'Pilot Academy [%s]: no path possible to Trade Target: %s. Going to find alternative.'.
            [@this.assignedcontrolled.debugname, @$tradeTarget.debugname]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>
        <do_if value="@$tradeTarget.dockingenabled != true or @$tradeTarget.dockingallowed.{this.assignedcontrolled} != true">
          <debug_text text="'Pilot Academy [%s]: no docking possible to trade target: %s (enabled: %s, allowed: %s), skipping.'.
          [@this.assignedcontrolled.debugname, @$tradeTarget.debugname, @$tradeTarget.dockingenabled, @$tradeTarget.dockingallowed.{this.assignedcontrolled}]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>

        <do_if value="@$tradeTarget.dock.{$shipDockSize} == null or @$tradeTarget.dock.{$shipDockSize}.isdockingallowed != true or @$tradeTarget.dock.{$shipDockSize}.todockpos == null">
          <debug_text text="'Pilot Academy [%s]: no docks of right size %s, or no docking allowed: %s, or no dock position %s at: %s, skipping.'.
          [@this.assignedcontrolled.debugname, @$tradeTarget.dock.{$shipDockSize}, @$tradeTarget.dock.{$shipDockSize}.isdockingallowed, @$tradeTarget.dock.{$shipDockSize}.todockpos, @$tradeTarget.debugname]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>
      </do_if>
      <do_else>
        <do_if value="$tradeOperation == 'sell'">
          <debug_text
            text="'Pilot Academy [%s]: completed selling %s of %s to %s' . [this.assignedcontrolled.debugname, $tradeAmountDefault, $tradeWare.id, $buyer.debugname]"
            chance="$debugchance" />
          <set_value name="$applyExperienceChance" exact="100" />
          <do_if value="this.combinedskill gt $targetCombinedSkill">
            <set_value name="$applyExperienceChance" exact="5" />
          </do_if>
          <apply_experience entity="this" experience="'ship_trade_good'" factor="1" chance="$applyExperienceChance" />
          <apply_experience object="this.assignedcontrolled" role="entityrole.service" experience="'ship_trade_good'" factor="1" chance="$applyExperienceChance" />
          <do_if value="$shipInfo.$dataIsUpdated">
            <debug_text text="'Pilot Academy [%s]: data is updated, going to re-init trade cycle' . [this.assignedcontrolled.debugname]" chance="$debugchance" />
            <set_value name="$shipInfo.$dataIsUpdated" exact="false" />
            <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
            <resume label="prepareTrade" />
          </do_if>
        </do_if>
        <include_interrupt_actions ref="PilotAcademyPrintStats" />
        <include_interrupt_actions ref="PilotAcademyReportRankUp" />
        <do_if value="@$rankUpReported == true">
          <wait exact="600ms" />
        </do_if>
        <do_if value="this.assignedcontrolled.commander == null and (player.age - $lastTradeInfoRefresh) ge $tradeInfoRefreshInterval">
          <debug_text
            text="'Pilot Academy [%s]: there is wing commander and trade info is stale (last refresh at %s, current time %s, refresh interval %s)' .
            [this.assignedcontrolled.debugname, $lastTradeInfoRefresh, player.age, $tradeInfoRefreshInterval]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>
        <set_value name="$tradeTarget" exact="$seller" />
        <set_value name="$targetType" exact="'seller'" />
        <set_value name="$tradeOperation" exact="'buy'" />
        <set_value name="$tradeOffer" exact="$selectedSellOffer" />
        <do_if value="this.assignedcontrolled.gatedistance.{$tradeTarget}.{$blacklistGroup}.{this.assignedcontrolled} lt 0">
          <debug_text text="'Pilot Academy [%s]: no path possible to Trade Target: %s. Going to find alternative.'.
            [@this.assignedcontrolled.debugname, @$tradeTarget.debugname]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>
        <debug_text
          text="'Pilot Academy [%s]: checking existing cargo %s before proceeding with buy trade operation.'. [this.assignedcontrolled.debugname, @this.assignedcontrolled.cargo.table]"
          chance="$debugchance" />
        <do_if value="@this.assignedcontrolled.cargo.count gt 0">
          <debug_text
            text="'Pilot Academy [%s]: clearing existing cargo %s before proceeding with buy trade operation.'. [this.assignedcontrolled.debugname, @this.assignedcontrolled.cargo.table]"
            chance="$debugchance" />
          <do_for_each name="$cargoWare" in="this.assignedcontrolled.cargo.list">
            <set_value name="$wareAmount" exact="this.assignedcontrolled.cargo.{$cargoWare}.count" />
            <debug_text text="'Pilot Academy [%s]: trying to remove cargo %s in amount %s' . [this.assignedcontrolled.debugname, $cargoWare, $wareAmount]" chance="$debugchance" />
            <remove_cargo object="this.assignedcontrolled" ware="$cargoWare" exact="$wareAmount" result="$removeResult" />
            <debug_text text="'Pilot Academy [%s]: removed cargo %s, result: %s, original amount: %s, not freed: %s.' . [this.assignedcontrolled.debugname, $cargoWare, $removeResult, $wareAmount, $removeResult lt $wareAmount]" chance="$debugchance" />
            <do_if value="@$removeResult lt $wareAmount">
              <debug_text text="'Pilot Academy [%s]: unable to remove all cargo %s, removed %s out of %s, going drop cargo and retry' . [this.assignedcontrolled.debugname, $cargoWare, $removeResult, $wareAmount]" chance="$debugchance" />
              <drop_cargo object="this.assignedcontrolled" ware="$cargoWare" exact="$wareAmount" />
              <debug_text text="'Pilot Academy [%s]: dropped cargo %s. In cargo: %s.' . [this.assignedcontrolled.debugname, $cargoWare, @this.assignedcontrolled.cargo.{$cargoWare}.count]" chance="$debugchance" />
            </do_if>
            <remove_value name="$wareAmount" />
            <remove_value name="$removeResult" />
          </do_for_each>
        </do_if>
        <debug_text text="'Pilot Academy [%s]: cargo for ware %s, free space amount %s' . [this.assignedcontrolled.debugname, $tradeWare.id, @this.assignedcontrolled.cargo.{$tradeWare}.free]" chance="$debugchance" />
      </do_else>

      <do_if value="@$tradeOffer.exists != true">
        <debug_text
          text="'Pilot Academy [%s]: trade offer no longer exists for %s %s offer of %s from %s' . [this.assignedcontrolled.debugname, $targetType, $tradeOperation, $tradeWare.id, $tradeOffer.owner.debugname]"
          chance="$debugchance" />
        <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
        <resume label="prepareTrade" />
      </do_if>

      <set_command command="command.trade" param="$tradeTarget" />
      <set_command_action commandaction="commandaction.flyingto" />

      <set_value name="$tradeAmount" exact="$tradeAmountDefault" />

      <do_if value="@$tradeOperation == 'buy'">
        <debug_text
          text="'Pilot Academy [%s]: preparing to buy %s of %s from %s' . [this.assignedcontrolled.debugname, $tradeAmountDefault, $tradeWare.id, $tradeTarget.debugname]"
          chance="$debugchance" />
        <add_ware_reservation object="$tradeTarget" type="buy" ware="$tradeWare" amount="$tradeAmount" reserver="this.assignedcontrolled" result="$reservationResult" />
        <debug_text text="'Pilot Academy [%s]: added ware %s reservation for buy trade, result: %s' . [this.assignedcontrolled.debugname, $tradeWare.id, $reservationResult]" chance="$debugchance" />
      </do_if>
      <do_elseif value="@$tradeOperation == 'sell'">
        <debug_text
          text="'Pilot Academy [%s]: preparing to sell %s of %s to %s' . [this.assignedcontrolled.debugname, $tradeAmountDefault, $tradeWare.id, $tradeTarget.debugname]"
          chance="$debugchance" />
        <add_ware_reservation object="$tradeTarget" type="sell" ware="$tradeWare" amount="$tradeAmount" reserver="this.assignedcontrolled" result="$reservationResult" />
        <debug_text text="'Pilot Academy [%s]: added ware %s reservation for sell trade, result: %s' . [this.assignedcontrolled.debugname, $tradeWare.id, $reservationResult]" chance="$debugchance" />
      </do_elseif>

      <label name="moveTo" />
      <set_value name="$isMovingToTradeTarget" exact="true" />
      <do_if value="@this.assignedcontrolled.dock.container != $tradeTarget">
        <debug_text text="'Pilot Academy [%s]: moving to dock on %s %s' . [this.assignedcontrolled.debugname, $targetType, $tradeTarget.debugname]"
          chance="$debugchance" />
        <run_script name="'order.dock'">
          <param name="destination" value="$tradeTarget" />
          <param name="dockfollowers" value="false" />
          <param name="debugchance" value="$debugchance" />
        </run_script>
        <set_value name="$lastUndockedStamp" exact="player.age" />
      </do_if>
      <do_if value="@this.assignedcontrolled.dock.container != $tradeTarget">
        <debug_text text="'Pilot Academy [%s]: still not reached %s %s' . [this.assignedcontrolled.debugname, $targetType, $tradeTarget.debugname]"
          chance="$debugchance" />
        <wait min="10ms" max="20ms" />
        <do_if value="$tradeTarget.owner.hasrelation.dock.{faction.player} == false">
          <debug_text
            text="'Pilot Academy [%s]: unable to dock on %s %s due to bad relations' . [this.assignedcontrolled.debugname, $targetType, $tradeTarget.debugname]"
            chance="$debugchance" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <resume label="prepareTrade" />
        </do_if>
        <resume label="moveTo" />
      </do_if>
      <set_value name="$isMovingToTradeTarget" exact="false" />
      <debug_text text="'Pilot Academy [%s]: reached %s %s' . [this.assignedcontrolled.debugname, $targetType, $tradeTarget.debugname]" chance="$debugchance" />

      <include_interrupt_actions ref="PilotAcademyInitShipVariables" />
      <include_interrupt_actions ref="PilotAcademyStoreCurrentShipInfo" />

      <do_if value="@$tradeOffer.exists != true">
        <debug_text
          text="'Pilot Academy [%s]: trade offer no longer exists for %s %s offer of %s from %s' . [this.assignedcontrolled.debugname, $targetType, $tradeOperation, $tradeWare.id, $tradeOffer.owner.debugname]"
          chance="$debugchance" />
        <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
        <resume label="prepareTrade" />
      </do_if>

      <debug_text
        text="'Pilot Academy [%s]: going to execute trade on: %s with: %s'.
          [this.assignedcontrolled.debugname, @$tradeTarget.debugname, $tradeWare.id]"
        chance="$debugchance" />
      <do_if value="@$targetType == 'seller'">
        <execute_custom_trade seller="$tradeTarget" buyer="this.assignedcontrolled" ware="$tradeWare" price="$tradeOffer.unitprice" amount="$tradeAmount" result="$tradeResult" sinceversion="5" />
      </do_if>
      <do_else>
        <execute_custom_trade seller="this.assignedcontrolled" buyer="$tradeTarget" ware="$tradeWare" price="$tradeOffer.unitprice" amount="$tradeAmount" result="$tradeResult" sinceversion="5" />
      </do_else>
      <debug_text
        text="'Pilot Academy [%s]: proceed to %s %s of %s from %s with result: %s.'.[this.assignedcontrolled.debugname, $tradeOperation, $tradeAmount, $tradeWare.id, $tradeTarget.debugname, $tradeResult]"
        chance="$debugchance" />

      <include_interrupt_actions ref="PilotAcademyCheckAndClearReservation" />

      <do_if value="$tradeResult le 0">
        <debug_text
          text="'Pilot Academy [%s]: unable to trade to %s on %s'.[this.assignedcontrolled.debugname, $tradeOperation, $tradeOffer.owner.debugname]"
          chance="$debugchance" />
        <do_if value="$tradeOperation == 'buy'">
          <clamp_trade_amount trade="$tradeOffer" amount="$tradeAmount" buyer="this.assignedcontrolled" seller="$tradeTarget" result="$checkAmount" unbundle="$tradeOffer.unbundle" updatedeal="false" reason="$checkReason" />
          <debug_text
            text="'Pilot Academy [%s]: clamping trade amount for buy operation, original amount: %s, checked amount: %s, reason: %s' .
            [this.assignedcontrolled.debugname, $tradeAmount, $checkAmount, $checkReason]"
            chance="$debugchance" />
          <do_if value="($checkReason == 'buyer_money' and player.money gt $tradeOffer.unitprice * 10 * $tradeAmount) or
            ($checkReason == 'buyer_cargo' and @this.assignedcontrolled.cargo.{$tradeWare}.free ge $tradeAmount)">
            <debug_text
              text="'Pilot Academy [%s]: something wrong with this ship. Will wait a bit and retry the trade. Check reason: %s, player money: %s, buyer money: %s, buyer cargo free space for ware: %s' .
              [this.assignedcontrolled.debugname, $checkReason, player.money, @this.assignedcontrolled.money, @this.assignedcontrolled.cargo.{$tradeWare}.free]"
              chance="$debugchance" />
            <wait min="33s" max="55s" />
            <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
            <include_interrupt_actions ref="PilotAcademyRemoveShipVariables" />
            <resume label="prepareTrade" />
          </do_if>
        </do_if>
        <do_if value="$tradeOperation == 'buy' or this.assignedcontrolled.cargo.{$cargoWare}.count gt 0">
          <set_value name="$tradeOperation" exact="''" />
          <do_if value="$targetType == 'seller'">
            <set_value name="$blackListAddOperation" exact="'sell'" />
            <set_value name="$blackListAddOffer" exact="$tradeOffer" />
          </do_if>
          <do_else>
            <set_value name="$blackListAddOperation" exact="'buy'" />
            <set_value name="$blackListAddOffer" exact="$tradeOffer" />
          </do_else>
          <include_interrupt_actions ref="PilotAcademyAddTradeToBlacklist" />
          <include_interrupt_actions ref="PilotAcademyResetCurrentTradeInfo" />
          <include_interrupt_actions ref="PilotAcademyRemoveShipVariables" />
          <resume label="prepareTrade" />
        </do_if>
      </do_if>


      <do_if value="$tradeOperation == 'sell'">
        <set_value name="$freeDocks" exact="0" />
        <find_dockingbay object="$tradeTarget" name="$targetDocks" multiple="true">
          <match_dock storage="false" free="true" size="this.assignedcontrolled.macro.docksize" />
        </find_dockingbay>
        <do_all exact="$targetDocks.count" counter="$d">
          <set_value name="$freeDocks" exact="$targetDocks.{$d}.shipstorage.free" operation="add" />
        </do_all>
        <debug_text
          text="'Pilot Academy [%s]: found %s free docks of right size at %s. Last undock: %s.' . [this.assignedcontrolled.debugname, $freeDocks, $tradeTarget.debugname, @$lastUndockedStamp]"
          chance="$debugchance" />
        <do_if value="$freeDocks le 2 or player.age - @$lastUndockedStamp gt 300s">
          <debug_text
            text="'Pilot Academy [%s]: not enough free docks for trade at %s, free docks: %s, or too long since last undock: %s, going to undock.' .
            [this.assignedcontrolled.debugname, $tradeTarget.debugname, $freeDocks, player.age - @$lastUndockedStamp]"
            chance="$debugchance" />
          <run_script name="'move.undock'">
            <param name="debugchance" value="$debugchance" />
          </run_script>
        </do_if>
      </do_if>

      <wait min="200ms" max="400ms" />


      <resume label="proceedWithTrade" />

      <label name="waitInfoFromCommander" />


      <set_command command="command.wait" />
      <set_command_action commandaction="commandaction.calculating" />
      <set_value name="$i" exact="[1,2,3,4,5].random" />
      <signal_objects object="this.assignedcontrolled.commander" param="'PilotAcademyRAndR.TradeInfoDataRequest'" param2="this.assignedcontrolled"
        delay="$i * 150ms" />
      <debug_text text="'Pilot Academy [%s]: request was sent, waiting now for trade info update from commander' . [this.assignedcontrolled.debugname]"
        chance="$debugchance" />
      <wait exact="60s">
        <interrupt>
          <conditions>
            <event_object_signalled object="this.assignedcontrolled" param="'PilotAcademyRAndR.WingDataIsUpdatedSignal'" />
          </conditions>
          <actions>
            <debug_text text="'Pilot Academy [%s]: received trade info update notification from commander' . [this.assignedcontrolled.debugname]"
              chance="$debugchance" />
            <set_value name="$tradeOperation" exact="''" />
            <resume label="prepareTrade" />
          </actions>
        </interrupt>
      </wait>
      <resume label="prepareTrade" />

      <label name="pilotReplacement" />
      <debug_text
        text="'Pilot Academy [%s]: pilot replacement requested at %s by %s' . [this.assignedcontrolled.debugname, player.age, @$replacementPilot.debugname]"
        chance="$debugchance" />
      <set_command command="command.wait" />
      <set_command_action commandaction="commandaction.standingby" />
      <do_if value="@$replacementPilot != null">
        <set_value name="$replacementPilotTemplate" exact="$replacementPilot.npctemplate" />
        <set_value name="$academyObject" exact="$replacementPilot.roleobject" />
        <debug_text
          text="'Pilot Academy [%s]: going to move the replacement pilot %s with template %s' .
            [this.assignedcontrolled.debugname, $replacementPilot.debugname, $replacementPilotTemplate]"
          chance="$debugchance" />
        <transfer_people result="$personTransferred" object="this.assignedcontrolled" otherobject="$academyObject"
          receiveworkforce="false">
          <existing_people people="[$replacementPilotTemplate]" />
        </transfer_people>
        <debug_text
          text="'Pilot Academy [%s]: Person transferred: Count: %s, Data: %s.'.
                            [ this.assignedcontrolled.debugname, $personTransferred.count, $personTransferred ]"
        />
        <do_if value="$personTransferred.count == 1">
          <debug_text
            text="'Pilot Academy [%s]: new pilot %s successfully transferred to %s. Going to swap with %s' .
            [this.assignedcontrolled.debugname, $replacementPilotTemplate, this.assignedcontrolled.debugname, this.debugname]"
            chance="$debugchance" />
          <signal_objects object="player.entity" param="'PilotAcademyRAndR.PilotSwapRequest'"
            param2="table[
              $target = this.assignedcontrolled,
              $newNPCTemplate = $personTransferred.{1},
              $academyObject = $academyObject,
              $isCadet = true,
              $iteration =  0
            ]"
            delay="200ms" />
          <remove_value name="$replacementPilotTemplate" />
          <remove_value name="$replacementPilot" />
          <resume label="pilotReplacementWait" />
        </do_if>
        <remove_value name="$replacementPilotTemplate" />
      </do_if>
      <remove_value name="$replacementPilot" />
      <resume label="prepareTrade" />


      <label name="pilotReplacementWait" />

      <wait min="3s" max="5s" />
      <resume label="pilotReplacementWait" />

      <label name="finish" />
      <debug_text
        text="'Pilot Academy [%s]: finished order.pilot.academy.wing at %s. Subordinates count: %s' . [this.assignedcontrolled.debugname, player.age, @this.assignedcontrolled.subordinates.count]"
        chance="$debugchance" />
      <do_for_each name="$subordinate" in="this.assignedcontrolled.subordinates">
        <debug_text text="'Pilot Academy [%s]: going to dismiss subordinate %s'.[this.assignedcontrolled.debugname, $subordinate.debugname]"
          chance="$debugchance" />
        <signal_objects object="$subordinate" param="'PilotAcademyRAndR.DismissWingRequest'" />
      </do_for_each>
      <include_interrupt_actions ref="PilotAcademyRemoveShipVariables" />
      <remove_object_commander object="this.assignedcontrolled" />
      <signal_objects object="player.entity" param="'AcademyPilotShipDeregisterRequest'" param2="this.assignedcontrolled" />
      <create_order object="this.assignedcontrolled" id="'Wait'" default="true" />

      <label name="abort" />
    </actions>
  </attention>
  <on_abort>
    <debug_text text="'Pilot Academy [%s]: aborted order.pilot.academy.wing at %s' . [@$currentShip.debugname, player.age]" chance="$debugchance" />
  </on_abort>
</aiscript>